# 首轮验证记录

日期：2026-09-18；范围：P0 工程基础。不代表 P1 真实关注核验或远程部署已完成。

## GitHub CI 实际结果

**应用代码提交 `babae1c28701a7957bb30ba044c5ac57c25480c2`：整条 CI 通过。**

[验证运行 #2](https://github.com/1264585648/knowledge/actions/runs/35364442979)，任务 `verify`，GitHub 返回 `completed / success`。

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| Node 核心测试 | 43 项通过 | 真实 SQLite 内存库 + 生产权限模块；ASSETS 使用替身 |
| 依赖安装 | 通过 | CI Node 22.23.2；固定 npm 11.6.4 |
| Astro 和 Worker 类型检查 | 通过 | npm run check |
| Vitest | 通过 | 同一组 43 项核心测试，不重复计为新增用例 |
| Astro 静态构建 | 通过 | 仅公开示例内容 |
| Wrangler + Playwright | 7 项通过 | 真实本地 Worker/静态资源、浏览器跳转、HTML/JSON/脚本直链拒绝、移动端无横向溢出 |

第一轮提交 `4f6c5ae7c2afa9ecfb799b7c17faa418837ae81c` 的核心测试通过，但 npm 10.9.8 安装依赖时出现 `Cannot read properties of null (reading 'edgesOut')`。将 CI 安装器固定为 npm 11.6.4 后，第二轮完整通过；未通过关闭测试、忽略失败或放开权限来规避问题。

浏览器端到端测试针对基础版本默认的 disabled 状态；**尚未进行真实第三方账号的关注成功/取消关注端到端验证**。核心测试中的有效授权来自进程内 SQLite 测试数据，不是生产后门。

## 本地实际结果

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| Node 核心测试 | 43 项通过，0 失败 | Node 22.16.0；真实 SQLite 内存库 |
| 核心 TypeScript 严格检查 | 通过 | 环境 TypeScript 5.8.3；worker/types.ts、auth.ts、handler.ts |
| 浏览器脚本/构建脚本语法 | 通过 | node --check；不等于本地浏览器执行 |

本地容器无法解析 github.com / registry.npmjs.org，所以完整安装和构建改由 GitHub CI 执行；本地没有伪造 Astro 构建、浏览器截图或远程部署结果。

核心测试覆盖有效授权；首页/文章 HTML/搜索 JSON/脚本/图片/附件直链拒绝；关闭关注配置；D1 缺失/异常；会话过期/撤销；用户停用；关注过期/撤销/未来时间；跨目标、跨身份、跨平台借用；重复/伪造 Cookie；明确公开资源；no-store 与条件请求；HEAD；自报关注拒绝；跨域退出拒绝；安全 Cookie；真实 SQL 外键/唯一/有效期约束；路径编码与跳转约束。

## 尚未完成的事项

当前仓库没有已提交的 package-lock.json。首次本机安装后应提交真实生成的锁文件；固定 npm 版本不等于已经锁定所有传递依赖。

真实关注渠道与账号权限核验、OAuth/回调/挑战消费、可靠关注授权签发、取关/复核策略、防滥用、私有内容构建自动拉取、真实 D1 迁移、密钥、域名与预览入口回归、访问额度/CPU 实测仍待后续阶段完成。测试不是完整的安全审计，也不是公网容量承诺。

基础版本部署命令故意失败，不提供虚假的开放访问按钮；未创建 Cloudflare 资源或生产账号。
