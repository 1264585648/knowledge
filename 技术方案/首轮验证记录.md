# 首轮验证记录

日期：2026-09-18；范围：P0 工程基础。不代表 P1 真实关注核验或远程部署已完成。

## 实际执行

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| Node 核心测试 | 43 项通过，0 失败 | Node 22.16.0；真实 SQLite 内存库 + 生产权限模块 |
| 核心 TypeScript 严格检查 | 通过 | 环境提供的 TypeScript 5.8.3；仅 worker/types.ts、auth.ts、handler.ts |
| 公开访问脚本与阅读脚本语法 | 通过 | node --check；非浏览器执行结果 |

测试覆盖：有效授权；首页/文章 HTML/搜索 JSON/脚本/图片/附件直链拒绝；关闭关注配置；D1 缺失/异常；会话过期/撤销；用户停用；关注过期/撤销/未来时间；跨目标、跨身份、跨平台借用；重复/伪造 Cookie；明确公开资源；no-store 与条件请求；HEAD；自报关注拒绝；跨域退出拒绝；安全 Cookie；真实 SQL 外键/唯一/有效期约束；路径编码与跳转约束。

测试中的 ASSETS 是受监控的替身，因此本轮核心通过不等于 Cloudflare 静态路由已经完成端到端验证。已另写 Playwright + Wrangler 测试用于覆盖这一层。

## 当前未执行

当前运行环境无法解析 github.com / registry.npmjs.org，常规 git clone 和 npm install 不可用。仓库内容通过 GitHub 连接器读取/提交。

因此完整依赖安装、Astro check/build、Hono 实际运行、Vitest、Playwright 和 Wrangler 构建/预览，不能记为本轮本地通过。工程提供只读 GitHub CI，联网环境可执行；CI 的最终状态应以对应提交实际结果为准。

尚无真实生成的 package-lock.json。首次安装后应提交锁文件并将 CI 的安装分支收敛为 npm ci，不能把当前状态称为完全可复现构建。

## 上线前仍需完成

真实关注渠道与账号权限核验、OAuth/回调/挑战消费、可靠关注授权签发、取关/复核策略、防滥用、私有内容构建、真实 D1 迁移、密钥、域名与预览入口回归、访问额度/CPU 实测。基础版本部署命令故意失败，不提供虚假的开放访问按钮。
